name: Must Watch — Generate

on:
  schedule:
    # Monday 10:00 UTC = 6:00 AM ET (EDT, UTC-4).
    # Adjust to "0 11 * * 1" in winter when EST (UTC-5) is active.
    - cron: "0 10 * * 1"
  workflow_dispatch:

jobs:
  generate:
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
      - name: Checkout main
        uses: actions/checkout@v4
        with:
          ref: main
          # Full history needed so we can branch cleanly from main
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Generate Must Watch page
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        # --sports is pinned to the two validated leagues. NFL and NCAAF are
        # implemented and unit-tested but have never run against a live ESPN
        # response — add them here only after a manual
        # `python run.py --diag --sports mlb,nba,nfl` confirms the payloads
        # parse and the cross-sport ranking looks sane.
        run: python run.py --auto --sports mlb,nba

      - name: Check for changes to index.html
        id: check_changes
        run: |
          if git diff --quiet index.html; then
            echo "changed=false" >> "$GITHUB_OUTPUT"
            echo "index.html unchanged — nothing to publish."
          else
            echo "changed=true" >> "$GITHUB_OUTPUT"
          fi

      - name: Commit and push to main
        if: steps.check_changes.outputs.changed == 'true'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add index.html
          git commit -m "chore: generate Must Watch — $(date -u '+%Y-%m-%d') [skip ci]"
          git push origin main
